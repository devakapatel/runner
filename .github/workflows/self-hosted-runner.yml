name: SSH Access via Cloudflare Tunnel

on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      TUNNEL_TOKEN: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y dropbear

      - name: Configure SSH server with key auth
        run: |
          sudo systemctl stop dropbear || true
          sudo systemctl disable dropbear || true
          sudo mkdir -p /etc/dropbear
          sudo rm -f /etc/dropbear/dropbear_rsa_host_key /etc/dropbear/dropbear_ecdsa_host_key /etc/dropbear/dropbear_dss_host_key
          sudo dropbearkey -t rsa -f /etc/dropbear/dropbear_rsa_host_key
          sudo dropbearkey -t ecdsa -f /etc/dropbear/dropbear_ecdsa_host_key
          sudo dropbearkey -t dss -f /etc/dropbear/dropbear_dss_host_key
          mkdir -p /home/runner/.ssh
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > /home/runner/.ssh/authorized_keys
          chmod 700 /home/runner/.ssh
          chmod 600 /home/runner/.ssh/authorized_keys
          chown -R runner:runner /home/runner/.ssh
          sudo dropbear -p 2222 -F -E &
          sleep 3
          ss -tlnp | grep :2222
          echo "SSH server listening on port 2222"

      - name: Start Cloudflare Tunnel
        run: |
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb
          cloudflared tunnel --no-autoupdate run --token "$TUNNEL_TOKEN" &
          sleep 10

      - name: Show connection info
        run: |
          echo "=== SSH Access Ready ==="
          echo "Cloudflare Tunnel TCP ingress: ssh.cluster.qzz.io -> tcp://localhost:2222"
          echo ""
          echo "To connect, you have TWO options:"
          echo ""
          echo "Option 1: Direct SSH (requires Cloudflare Access SSH app in dashboard)"
          echo "  ssh runner@ssh.cluster.qzz.io"
          echo ""
          echo "Option 2: TCP tunnel with cloudflared client (works with current setup)"
          echo "  # Terminal 1: Run cloudflared locally"
          echo "  cloudflared access tcp --hostname ssh.cluster.qzz.io --url localhost:2222"
          echo "  # Terminal 2: Connect via local tunnel"
          echo "  ssh -p 2222 runner@localhost"
          echo ""
          echo "Timeout: 360 minutes"
          sleep 21600